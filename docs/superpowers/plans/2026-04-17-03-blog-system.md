# 博客系统实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现博客系统，包括 Markdown 解析、博客列表页、博客详情页、标签筛选和搜索功能。

**Architecture:** 博客文章存储在 `public/posts/` 目录的 Markdown 文件中，使用 marked 解析 Markdown，highlight.js 实现代码高亮。通过 fetch API 读取文件，按时间倒序显示。

**Tech Stack:** marked, highlight.js, Vue 3 Composition API, Vue Router

---

## 文件结构

| 文件 | 职责 |
|------|------|
| `src/utils/markdown.js` | Markdown 解析和代码高亮工具 |
| `src/utils/date.js` | 日期格式化工具 |
| `src/utils/fetch.js` | 博客文章获取工具 |
| `src/components/blog/BlogCard.vue` | 博客文章卡片 |
| `src/components/blog/BlogList.vue` | 博客列表容器 |
| `src/components/blog/BlogDetail.vue` | 博客详情容器 |
| `src/pages/Blog.vue` | 博客列表页 |
| `src/pages/BlogDetail.vue` | 博客详情页 |
| `public/posts/*.md` | 博客文章 Markdown 文件 |

---

### Task 1: 安装 Markdown 和代码高亮依赖

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 更新 package.json 添加依赖**

```json
{
  "name": "personal-blog",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.5",
    "lucide-vue-next": "^0.309.0",
    "marked": "^12.0.0",
    "highlight.js": "^11.9.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.0"
  }
}
```

- [ ] **Step 2: 运行 npm install**

Run: `npm install`
Expected: marked 和 highlight.js 安装成功

- [ ] **Step 3: 提交**

```bash
git add package.json package-lock.json
git commit -m "chore: 添加 Markdown 和代码高亮依赖"
```

---

### Task 2: 创建日期格式化工具

**Files:**
- Create: `src/utils/date.js`

- [ ] **Step 1: 创建 date.js**

```javascript
export function formatDate(dateString) {
  const date = new Date(dateString)
  const options = { year: 'numeric', month: 'long', day: 'numeric' }
  return date.toLocaleDateString('zh-CN', options)
}

export function formatRelativeTime(dateString) {
  const date = new Date(dateString)
  const now = new Date()
  const diff = now - date
  const days = Math.floor(diff / (1000 * 60 * 60 * 24))

  if (days === 0) return '今天'
  if (days === 1) return '昨天'
  if (days < 7) return `${days} 天前`
  if (days < 30) return `${Math.floor(days / 7)} 周前`
  if (days < 365) return `${Math.floor(days / 30)} 个月前`
  return `${Math.floor(days / 365)} 年前`
}
```

- [ ] **Step 2: 提交**

```bash
git add src/utils/date.js
git commit -m "chore: 创建日期格式化工具"
```

---

### Task 3: 创建 Markdown 解析工具

**Files:**
- Create: `src/utils/markdown.js`

- [ ] **Step 1: 创建 markdown.js**

```javascript
import { marked } from 'marked'
import hljs from 'highlight.js'

// 配置 marked
marked.setOptions({
  highlight: function(code, lang) {
    if (lang && hljs.getLanguage(lang)) {
      try {
        return hljs.highlight(code, { language: lang }).value
      } catch (err) {
        console.error(err)
      }
    }
    return hljs.highlightAuto(code).value
  },
  langPrefix: 'hljs language-',
  breaks: true,
  gfm: true
})

/**
 * 解析 Frontmatter
 * @param {string} content - Markdown 内容
 * @returns {Object} { metadata, content }
 */
export function parseFrontmatter(content) {
  const frontmatterRegex = /^---\n([\s\S]*?)\n---\n([\s\S]*)$/
  const match = content.match(frontmatterRegex)

  if (!match) {
    return { metadata: {}, content }
  }

  const frontmatter = match[1]
  const body = match[2]
  const metadata = {}

  frontmatter.split('\n').forEach(line => {
    const [key, ...valueParts] = line.split(':')
    if (key && valueParts.length > 0) {
      let value = valueParts.join(':').trim()

      // 移除引号
      if ((value.startsWith('"') && value.endsWith('"')) ||
          (value.startsWith("'") && value.endsWith("'"))) {
        value = value.slice(1, -1)
      }

      // 解析数组
      if (value.startsWith('[') && value.endsWith(']')) {
        value = value.slice(1, -1).split(',').map(v => v.trim().replace(/['"]/g, ''))
      }

      metadata[key.trim()] = value
    }
  })

  return { metadata, content: body }
}

/**
 * 渲染 Markdown 为 HTML
 * @param {string} markdown - Markdown 内容
 * @returns {string} HTML
 */
export function renderMarkdown(markdown) {
  return marked.parse(markdown)
}

/**
 * 解析完整的博客文章
 * @param {string} content - 带有 Frontmatter 的 Markdown
 * @returns {Object} { metadata, html }
 */
export function parsePost(content) {
  const { metadata, content: body } = parseFrontmatter(content)
  const html = renderMarkdown(body)
  return { metadata, html }
}
```

- [ ] **Step 2: 提交**

```bash
git add src/utils/markdown.js
git commit -m "chore: 创建 Markdown 解析工具"
```

---

### Task 4: 创建博客文章获取工具

**Files:**
- Create: `src/utils/fetch.js`

- [ ] **Step 1: 创建 fetch.js**

```javascript
import { parsePost } from './markdown.js'

const POSTS_INDEX = '/posts/index.json'

/**
 * 获取所有博客文章的索引
 * @returns {Promise<Array>} 文章列表
 */
export async function fetchPostsIndex() {
  try {
    const response = await fetch(POSTS_INDEX)
    if (!response.ok) throw new Error('Failed to fetch posts index')
    return await response.json()
  } catch (error) {
    console.error('Error fetching posts index:', error)
    return []
  }
}

/**
 * 根据 slug 获取单篇文章
 * @param {string} slug - 文章 slug
 * @returns {Promise<Object|null>} 文章内容
 */
export async function fetchPostBySlug(slug) {
  try {
    const response = await fetch(`/posts/${slug}.md`)
    if (!response.ok) throw new Error('Post not found')
    const content = await response.text()
    const { metadata, html } = parsePost(content)
    return {
      slug,
      ...metadata,
      html
    }
  } catch (error) {
    console.error('Error fetching post:', error)
    return null
  }
}

/**
 * 搜索文章
 * @param {Array} posts - 文章列表
 * @param {string} query - 搜索关键词
 * @returns {Array} 匹配的文章
 */
export function searchPosts(posts, query) {
  if (!query.trim()) return posts

  const lowerQuery = query.toLowerCase()
  return posts.filter(post =>
    post.title.toLowerCase().includes(lowerQuery) ||
    (post.summary && post.summary.toLowerCase().includes(lowerQuery)) ||
    (post.tags && post.tags.some(tag => tag.toLowerCase().includes(lowerQuery)))
  )
}

/**
 * 按标签筛选文章
 * @param {Array} posts - 文章列表
 * @param {string} tag - 标签
 * @returns {Array} 匹配的文章
 */
export function filterPostsByTag(posts, tag) {
  if (!tag) return posts
  return posts.filter(post =>
    post.tags && post.tags.includes(tag)
  )
}

/**
 * 获取所有标签
 * @param {Array} posts - 文章列表
 * @returns {Array} 标签列表
 */
export function getAllTags(posts) {
  const tags = new Set()
  posts.forEach(post => {
    if (post.tags) {
      post.tags.forEach(tag => tags.add(tag))
    }
  })
  return Array.from(tags).sort()
}
```

- [ ] **Step 2: 提交**

```bash
git add src/utils/fetch.js
git commit -m "chore: 创建博客文章获取工具"
```

---

### Task 5: 创建示例博客文章

**Files:**
- Create: `public/posts/hello-world.md`
- Create: `public/posts/vue3-composition-api.md`
- Create: `public/posts/tailwind-css-guide.md`
- Create: `public/posts/index.json`

- [ ] **Step 1: 创建 hello-world.md**

```markdown
---
title: "欢迎来到我的博客"
date: "2026-04-14"
tags: ["生活", "开篇"]
summary: "这是我的第一篇博客文章，欢迎来到我的个人博客！"
---

# 欢迎来到我的博客

这是我的第一篇博客文章，很高兴见到你！

## 关于这个博客

这是一个简洁的个人博客，我会在这里分享：

- 生活日常
- 技术文章
- 有趣的发现

## 代码示例

这是一个 JavaScript 代码块示例：

\`\`\`javascript
function greet(name) {
  return `Hello, ${name}!`
}

console.log(greet('World'))
\`\`\`

## 联系我

如果你想联系我，可以通过邮箱或者社交媒体。

感谢你的访问！
```

- [ ] **Step 2: 创建 vue3-composition-api.md**

```markdown
---
title: "Vue 3 Composition API 入门指南"
date: "2026-04-12"
tags: ["Vue", "前端", "教程"]
summary: "详细介绍 Vue 3 Composition API 的核心概念和使用方法。"
---

# Vue 3 Composition API 入门指南

Composition API 是 Vue 3 的一个重要特性，它提供了一种更灵活的方式来组织组件逻辑。

## setup() 函数

\`\`\`javascript
import { ref, computed } from 'vue'

export default {
  setup() {
    const count = ref(0)
    const double = computed(() => count.value * 2)

    function increment() {
      count.value++
    }

    return {
      count,
      double,
      increment
    }
  }
}
\`\`\`

## 响应式系统

Vue 3 的响应式系统基于 Proxy，提供了更好的性能和更少的限制。

## 总结

Composition API 让代码更易于复用和测试，建议在新项目中使用。
```

- [ ] **Step 3: 创建 tailwind-css-guide.md**

```markdown
---
title: "Tailwind CSS 快速入门"
date: "2026-04-10"
tags: ["CSS", "前端", "工具"]
summary: "学习 Tailwind CSS 的基本用法和最佳实践。"
---

# Tailwind CSS 快速入门

Tailwind CSS 是一个实用的 CSS 框架，让你可以快速构建现代网站。

## 安装

\`\`\`bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
\`\`\`

## 基本用法

\`\`\`html
<div class="bg-blue-500 text-white p-4 rounded-lg">
  Hello Tailwind!
</div>
\`\`\`

## 响应式设计

Tailwind 提供了强大的响应式设计支持。

## 总结

Tailwind CSS 让你不必离开 HTML 就能构建完全定制的网站设计。
```

- [ ] **Step 4: 创建 index.json**

```json
[
  {
    "slug": "hello-world",
    "title": "欢迎来到我的博客",
    "date": "2026-04-14",
    "tags": ["生活", "开篇"],
    "summary": "这是我的第一篇博客文章，欢迎来到我的个人博客！"
  },
  {
    "slug": "vue3-composition-api",
    "title": "Vue 3 Composition API 入门指南",
    "date": "2026-04-12",
    "tags": ["Vue", "前端", "教程"],
    "summary": "详细介绍 Vue 3 Composition API 的核心概念和使用方法。"
  },
  {
    "slug": "tailwind-css-guide",
    "title": "Tailwind CSS 快速入门",
    "date": "2026-04-10",
    "tags": ["CSS", "前端", "工具"],
    "summary": "学习 Tailwind CSS 的基本用法和最佳实践。"
  }
]
```

- [ ] **Step 5: 提交**

```bash
git add public/posts/
git commit -m "feat: 创建示例博客文章"
```

---

### Task 6: 创建 BlogCard 组件

**Files:**
- Create: `src/components/blog/BlogCard.vue`

- [ ] **Step 1: 创建 BlogCard.vue**

```vue
<template>
  <article class="card p-6 hover:shadow-lg transition-all duration-300 ease-out-cubic hover:-translate-y-1">
    <router-link :to="`/blog/${post.slug}`" class="block">
      <div class="flex flex-wrap gap-2 mb-3">
        <span
          v-for="tag in post.tags"
          :key="tag"
          class="px-2 py-1 text-xs bg-accent/10 text-accent rounded-full"
        >
          {{ tag }}
        </span>
      </div>

      <h2 class="font-display text-xl font-semibold text-primary mb-2 hover:text-accent transition-colors">
        {{ post.title }}
      </h2>

      <p class="text-secondary text-sm mb-4 line-clamp-2">
        {{ post.summary }}
      </p>

      <div class="flex items-center justify-between text-sm text-secondary">
        <time :datetime="post.date">
          {{ formatDate(post.date) }}
        </time>
        <span class="text-accent">阅读更多 →</span>
      </div>
    </router-link>
  </article>
</template>

<script setup>
import { formatDate } from '../../utils/date.js'

const props = defineProps({
  post: {
    type: Object,
    required: true
  }
})
</script>
```

- [ ] **Step 2: 提交**

```bash
git add src/components/blog/BlogCard.vue
git commit -m "feat: 创建 BlogCard 组件"
```

---

### Task 7: 创建 BlogList 组件

**Files:**
- Create: `src/components/blog/BlogList.vue`

- [ ] **Step 1: 创建 BlogList.vue**

```vue
<template>
  <div>
    <div class="mb-8 flex flex-col md:flex-row gap-4 justify-between items-start md:items-center">
      <div>
        <h1 class="font-display text-3xl font-bold text-primary mb-2">博客文章</h1>
        <p class="text-secondary">分享我的思考和发现</p>
      </div>

      <div class="flex flex-col sm:flex-row gap-4 w-full md:w-auto">
        <input
          v-model="searchQuery"
          type="search"
          placeholder="搜索文章..."
          class="px-4 py-2 rounded-input border border-border bg-background text-foreground focus:outline-none focus:ring-2 focus:ring-accent/50"
        />

        <select
          v-model="selectedTag"
          class="px-4 py-2 rounded-input border border-border bg-background text-foreground focus:outline-none focus:ring-2 focus:ring-accent/50"
        >
          <option value="">所有标签</option>
          <option v-for="tag in allTags" :key="tag" :value="tag">
            {{ tag }}
          </option>
        </select>
      </div>
    </div>

    <div v-if="loading" class="text-center py-12">
      <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-accent mx-auto"></div>
      <p class="text-secondary mt-4">加载中...</p>
    </div>

    <div v-else-if="filteredPosts.length === 0" class="text-center py-12">
      <p class="text-secondary">没有找到相关文章</p>
    </div>

    <div v-else class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      <BlogCard
        v-for="post in filteredPosts"
        :key="post.slug"
        :post="post"
      />
    </div>
  </div>
</template>

<script setup>
import { ref, computed, onMounted } from 'vue'
import { fetchPostsIndex, searchPosts, filterPostsByTag, getAllTags } from '../../utils/fetch.js'
import BlogCard from './BlogCard.vue'

const posts = ref([])
const loading = ref(true)
const searchQuery = ref('')
const selectedTag = ref('')

const allTags = computed(() => getAllTags(posts.value))

const filteredPosts = computed(() => {
  let result = posts.value

  if (searchQuery.value) {
    result = searchPosts(result, searchQuery.value)
  }

  if (selectedTag.value) {
    result = filterPostsByTag(result, selectedTag.value)
  }

  return result
})

onMounted(async () => {
  try {
    posts.value = await fetchPostsIndex()
  } catch (error) {
    console.error('Failed to load posts:', error)
  } finally {
    loading.value = false
  }
})
</script>
```

- [ ] **Step 2: 提交**

```bash
git add src/components/blog/BlogList.vue
git commit -m "feat: 创建 BlogList 组件"
```

---

### Task 8: 创建 BlogDetail 组件

**Files:**
- Create: `src/components/blog/BlogDetail.vue`

- [ ] **Step 1: 创建 BlogDetail.vue**

```vue
<template>
  <article class="max-w-3xl mx-auto">
    <div v-if="loading" class="text-center py-12">
      <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-accent mx-auto"></div>
      <p class="text-secondary mt-4">加载中...</p>
    </div>

    <div v-else-if="!post" class="text-center py-12">
      <p class="text-secondary">文章未找到</p>
      <router-link to="/blog" class="btn bg-accent hover:bg-blue-600 text-white mt-4">
        返回博客列表
      </router-link>
    </div>

    <div v-else>
      <router-link
        to="/blog"
        class="inline-flex items-center text-secondary hover:text-accent mb-6 transition-colors"
      >
        <ArrowLeft class="w-4 h-4 mr-2" />
        返回列表
      </router-link>

      <header class="mb-8">
        <div class="flex flex-wrap gap-2 mb-4">
          <span
            v-for="tag in post.tags"
            :key="tag"
            class="px-3 py-1 text-sm bg-accent/10 text-accent rounded-full"
          >
            {{ tag }}
          </span>
        </div>

        <h1 class="font-display text-3xl md:text-4xl font-bold text-primary mb-4">
          {{ post.title }}
        </h1>

        <div class="flex items-center text-secondary text-sm">
          <Calendar class="w-4 h-4 mr-2" />
          <time :datetime="post.date">{{ formatDate(post.date) }}</time>
        </div>
      </header>

      <div
        class="prose prose-lg dark:prose-invert max-w-none"
        v-html="post.html"
      ></div>

      <div class="mt-12 pt-8 border-t border-border">
        <router-link
          to="/blog"
          class="btn bg-muted hover:bg-secondary text-primary"
        >
          <ArrowLeft class="w-4 h-4 mr-2 inline" />
          返回博客列表
        </router-link>
      </div>
    </div>
  </article>
</template>

<script setup>
import { ref, onMounted, watch } from 'vue'
import { useRoute } from 'vue-router'
import { fetchPostBySlug } from '../../utils/fetch.js'
import { formatDate } from '../../utils/date.js'
import { ArrowLeft, Calendar } from 'lucide-vue-next'

const route = useRoute()
const post = ref(null)
const loading = ref(true)

const loadPost = async (slug) => {
  loading.value = true
  try {
    post.value = await fetchPostBySlug(slug)
  } catch (error) {
    console.error('Failed to load post:', error)
    post.value = null
  } finally {
    loading.value = false
  }
}

onMounted(() => {
  loadPost(route.params.slug)
})

watch(() => route.params.slug, (newSlug) => {
  if (newSlug) {
    loadPost(newSlug)
  }
})
</script>
```

- [ ] **Step 2: 提交**

```bash
git add src/components/blog/BlogDetail.vue
git commit -m "feat: 创建 BlogDetail 组件"
```

---

### Task 9: 创建博客列表页面

**Files:**
- Create: `src/pages/Blog.vue`

- [ ] **Step 1: 创建 Blog.vue**

```vue
<template>
  <div class="min-h-screen">
    <section class="py-12">
      <BlogList />
    </section>
  </div>
</template>

<script setup>
import BlogList from '../components/blog/BlogList.vue'
</script>
```

- [ ] **Step 2: 提交**

```bash
git add src/pages/Blog.vue
git commit -m "feat: 创建博客列表页面"
```

---

### Task 10: 创建博客详情页面

**Files:**
- Create: `src/pages/BlogDetail.vue`

- [ ] **Step 1: 创建 BlogDetail.vue**

```vue
<template>
  <div class="min-h-screen py-12">
    <BlogDetail />
  </div>
</template>

<script setup>
import BlogDetail from '../components/blog/BlogDetail.vue'
</script>
```

- [ ] **Step 2: 提交**

```bash
git add src/pages/BlogDetail.vue
git commit -m "feat: 创建博客详情页面"
```

---

### Task 11: 更新路由配置

**Files:**
- Modify: `src/router/index.js`

- [ ] **Step 1: 修改 router/index.js 添加博客路由**

```javascript
import { createRouter, createWebHistory } from 'vue-router'
import Home from '../pages/Home.vue'
import Blog from '../pages/Blog.vue'
import BlogDetail from '../pages/BlogDetail.vue'
import NotFound from '../pages/NotFound.vue'

const routes = [
  {
    path: '/',
    name: 'home',
    component: Home
  },
  {
    path: '/blog',
    name: 'blog',
    component: Blog
  },
  {
    path: '/blog/:slug',
    name: 'blog-detail',
    component: BlogDetail
  },
  {
    path: '/:pathMatch(.*)*',
    name: 'not-found',
    component: NotFound
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes,
  scrollBehavior(to, from, savedPosition) {
    if (savedPosition) {
      return savedPosition
    }
    return { top: 0 }
  }
})

export default router
```

- [ ] **Step 2: 提交**

```bash
git add src/router/index.js
git commit -m "chore: 添加博客路由"
```

---

### Task 12: 添加 Markdown 内容样式

**Files:**
- Modify: `src/styles/base.css`

- [ ] **Step 1: 在 base.css 添加 prose 样式**

```css
@import './variables.css';

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    font-family: var(--font-body);
    background-color: var(--color-background);
    color: var(--color-foreground);
    line-height: 1.6;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: var(--font-display);
  }

  body {
    min-height: 100vh;
  }
}

@layer components {
  .btn {
    @apply px-4 py-2 rounded-button font-medium transition-all duration-200 ease-out-cubic;
  }

  .card {
    @apply bg-white dark:bg-zinc-900 rounded-card border border-border dark:border-zinc-800;
  }

  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
}

@layer utilities {
  .prose {
    @apply max-w-none text-foreground;

    /* Typography */
    h1 { @apply font-display text-3xl font-bold mt-8 mb-4 text-primary; }
    h2 { @apply font-display text-2xl font-bold mt-6 mb-3 text-primary; }
    h3 { @apply font-display text-xl font-semibold mt-4 mb-2 text-primary; }

    /* Paragraphs */
    p { @apply mb-4 text-secondary leading-relaxed; }

    /* Links */
    a { @apply text-accent hover:underline; }

    /* Lists */
    ul, ol { @apply mb-4 pl-6 text-secondary; }
    li { @apply mb-2; }

    /* Blockquotes */
    blockquote {
      @apply border-l-4 border-accent pl-4 italic text-secondary my-4;
    }

    /* Code */
    code {
      @apply bg-muted px-1.5 py-0.5 rounded text-sm font-mono text-accent;
    }

    pre {
      @apply bg-muted rounded-lg p-4 overflow-x-auto my-4;
    }

    pre code {
      @apply bg-transparent px-0 py-0 text-foreground;
    }

    /* Tables */
    table {
      @apply w-full border-collapse my-4;
    }

    th, td {
      @apply border border-border px-4 py-2 text-left;
    }

    th {
      @apply font-semibold bg-muted;
    }

    /* Horizontal Rule */
    hr {
      @apply border-border my-8;
    }

    /* Images */
    img {
      @apply rounded-lg my-4 max-w-full h-auto;
    }
  }
}
```

- [ ] **Step 2: 提交**

```bash
git add src/styles/base.css
git commit -m "chore: 添加 Markdown 内容样式"
```

---

### Task 13: 验证博客功能

**Files:**
- None

- [ ] **Step 1: 启动开发服务器**

Run: `npm run dev`
Expected: 服务器在 http://localhost:3000 启动

- [ ] **Step 2: 验证功能**

- [ ] 访问 /blog 显示文章列表
- [ ] 点击文章卡片跳转到详情页
- [ ] 详情页正确渲染 Markdown 内容
- [ ] 代码块正确高亮
- [ ] 搜索功能正常工作
- [ ] 标签筛选功能正常工作
- [ ] 返回列表按钮正常工作
- [ ] 响应式布局正常

- [ ] **Step 3: 提交**

```bash
git add -A
git commit -m "test: 验证博客功能"
```

---

## 验收标准

- [ ] 博客列表页正确显示所有文章
- [ ] 博客详情页正确渲染 Markdown 内容
- [ ] 代码块正确高亮显示
- [ ] 搜索功能按标题、摘要、标签搜索
- [ ] 标签筛选功能正常
- [ ] 文章按时间倒序显示
- [ ] 响应式布局正常
- [ ] 返回导航正常

---

## 下一步

完成本模块后，继续执行 **2026-04-17-04-bookmark-system.md**（收藏系统）
